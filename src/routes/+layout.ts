export const prerender = true;
export const ssr = true;

// KEEP THIS ON! Removing CSR will break app's interactivity and cause hydration errors!!!
export const csr = true;

export const trailingSlash = 'never';